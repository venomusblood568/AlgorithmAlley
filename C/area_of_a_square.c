# include<stdio.h>

int main(){
    int side;
    printf("enter the size length in m: ");
    scanf("%d",&side);

    int area_square = side * side;
    printf ("area of the square: %d \n",area_square);
    return 0;
}
